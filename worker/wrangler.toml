name = "gruppenausfahrt-relay"
main = "relay.js"
compatibility_date = "2026-01-01"

[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

# Free plan: Durable Objects only with the SQLite backend.
# "new_classes" would be rejected here with error 10097.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
